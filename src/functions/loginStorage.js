export const setMealsTokenInStorage = (token) => localStorage.setItem('token', token);

export const setCocktailsTokenInStorage = (token) => localStorage.setItem('token', token);

export const setEmailInStorage = (email) => localStorage.setItem('email', email);
